#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello Tech people!Its Hacktoberfest.lets rock together.";

    int length = strlen(str);

    printf("Length of the string: %d\n", length);

    return 0;
}

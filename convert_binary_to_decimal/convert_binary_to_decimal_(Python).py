import math

binarynum = input("Enter the binary number to convert (including 8 numbers): ")

print("The binary number after being converted to decimal is: ", int(binarynum, 2))
print("--------------------------------")
